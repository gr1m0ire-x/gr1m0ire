import { supabase } from "./supabase.js"; const form=document.querySelector("#login-form"),msg=document.querySelector("#message"); const {data:{session}}=await supabase.auth.getSession(); if(session) location.href="dashboard.html"; form.addEventListener("submit",async e=>{e.preventDefault();msg.textContent="logging in...";const {error}=await supabase.auth.signInWithPassword({email:email.value,password:password.value});if(error){msg.textContent=error.message;return}location.href="dashboard.html"});